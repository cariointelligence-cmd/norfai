# Intelligence iteration report

- Project: Norf
- Date: 2026-09-12
- Environment: preview
- Cycle: Contact harvest blocked by fake directory websites
- Skill version: 3.1

## A. User can now

New searches no longer store Kauppalehti / Alma Talent CDN URLs as the company website, so contact harvest can run against the real site. Previous searches show a per-run button that drops those fake URLs and re-runs enrich + scrape to find published emails that were missed.

## B. Implemented

- Files: src/lib/norr/normalize.ts, sources/webdiscover.ts, sources/kauppalehti.ts, pipeline.ts, security.ts, actions.ts, routes/_app/search/$runId.tsx, norr.test.ts
- Migrations: none
- Jobs: enrich + scrape re-queued on the existing run (cap 250). No invented mailboxes.

Root cause: Kauppalehti HTML URL scrape accepted `https://assets.almatalent.fi/...`. persistHits wrote that as website. harvest then hit the CDN, SITE NOT FOUND, Email Not found. Phones and CEOs still came from directory cards.

## C. Sources and repos

No new sources. This slice is Immune (junk website denylist) + Sensors (Kauppalehti parser) + Nerve (re-enrich on the same run) + Face (button, hide CDN URLs).

## D. Tests

| Test | Type | Result |
|------|------|--------|
| isDirectoryHost(assets.almatalent.fi) | unit | pass |
| canonicalCompanyWebsite rejects Alma Talent PNG | unit | pass |
| parseKauppalehtiHtml keeps consti.fi, drops almatalent | unit | pass |
| norr+security+search-hardening+ux+admin-users | unit | 139 pass / 0 fail |

## E. Delta vs baseline

FALSE_WEBSITE: directory CDN stored as company site. Harvest never saw a real origin. Fixed going forward. Past rows stay until the run button or a later contact refresh clears them.

Display now nulls junk websites on getRun so Alma Talent hosts never render as the company site.

## F. Incomplete or blocked

- Live re-enrich against production rows not executed in this sandbox (no user session here)
- Emails stay Not found when the company does not publish one. inferGeneralMailbox exists and is still not called on this path on purpose
- Eval metrics still null
- Search-run chrome is still mostly English; the new button is fi/en/sv

## G. Next highest-value change

Keep YTJ pagination filling the requested count until the register is exhausted. Do not invent mailboxes.

## Honesty box

- Planned: reject fake websites; re-enrich missed emails on previous searches
- Implemented: denylist + persistHits guard + run-scoped re-enrich button + Face hide
- Verified: unit tests for Alma Talent rejection and Kauppalehti parse (this slice)
- Not production-ready because: live harvest of those three companies was not run here; published-email yield is still source-dependent
