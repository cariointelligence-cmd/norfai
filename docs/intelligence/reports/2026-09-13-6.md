# Intelligence iteration report

- Project: Norf
- Date: 2026-09-13
- Environment: preview
- Cycle: Register search flicker + MATCHED 0 while 27114 in register
- Skill version: 3.1

## A. User can now

Start a Finnish professional-services search (TOL 69|70|71|74) and see companies appear in MATCHED during the first register slice. Progress stays on Registers (~45%) instead of flickering 3% ↔ 45%. Search diagnosis NO_MATCH is not shown while the run is still searching.

## B. Implemented

- Claim the discover job in `startSearch` so Face ticks and the worker cannot run a second discover on the same run
- Attach YTJ rows as they pass the gate (`onKeep`) instead of holding them until the slice budget ends
- Do not write NO_MATCH / FILTER_EXCLUDED_ALL unless the slice is complete; Face drops those rows while status is running
- Queued discover on a live run counts as 45% Registers (same as running) so the rail cannot bounce 3 ↔ 45
- If a run stored 0 while `registerHits` > 0 and nothing was skip-seen excluded, reset the YTJ cursor once (STATUS3=2 rescan)
- Tick budget 15s ≥ discover budget 10s; stale-lock steal 30s; startSearch enrich tick skips discover
- Website from published company mailbox still DERIVED only; consumer/ISP/directory hosts stay UNKNOWN

## C. Tests

| Test | Type | Result |
|------|------|--------|
| queued vs running discover progress ~45%, delta ≤5 | unit | pass |
| diagnoseRegisterEmpty complete=false → null | unit | pass |
| shouldResetDiscoverCursor 27114/0 kept | unit | pass |
| dropPrematureDiagnosis hides NO_MATCH while running | unit | pass |
| tickMaxMs ≥ discoverBudgetMs | unit | pass |
| norr+engines+security+hardening+ux+quota | unit | 159+ pass / 0 fail |
| live ytjDiscover 69–74 cap 12 with onKeep | live PRH | 12 attached in 3s, registerHits 27114 (Borenius, Castrén & Snellman, tilitoimistot) |

## Honesty box

- Planned: stop MATCHED 0 and 3↔45 flicker on the in-flight 69–74 search
- Implemented: claim, attach-as-we-go, no premature NO_MATCH, progress lock, one-time cursor reset, tick/discover budget alignment
- Verified: unit tests + live PRH onKeep attach in this environment
- Not production-ready because: norfai.com lags this preview. Run `a31b85e4` was started on the old STATUS3 filter; press **Run again** after this build. Do not wait for that empty run to fill.
