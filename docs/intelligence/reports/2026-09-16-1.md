# Intelligence iteration report

- Project: Norf
- Date: 2026-09-16
- Environment: sandbox → Vercel production
- Cycle: hive mesh coordination + no full fan-out
- Skill version: 3.1

## A. User can now

Search uses one hive mesh. 642k combinatorial engines are addressable. A normal enrich picks 4–6 nation crawlers and skips the rest when email+phone+people already exist. Supercrawl no longer runs on every company.

## B. V1 gaps fixed

- Hive coordinator was a plan-only label; mesh now selects real crawler IDs.
- `supercrawlContacts` always ran after first-party harvest even when CORE_SALES already had a published mailbox.
- Northdata ran on every deep/recovery path even with emails found.

## C. Sources that actually work

Unchanged. Mesh routes existing first-party fleets (FI/SE/NO), 020202, YTJ hydrate, Kauppalehti, hypercrawl, DM paths. Apify stays skip-by-default.

## D. Engines wired

| Engine | Entrypoint | Persistent state | Callers | Status |
|--------|-----------|------------------|---------|--------|
| hive mesh | hive-mesh.ts hiveSelectEngines | none (pure) | pipeline runEnrich, hiveSourceReport | VERIFIED unit |
| nation fleet | runNationFleet only/limit | intel-cache | supercrawlContacts | VERIFIED unit |
| contact harvest early-exit | contactHarvestDone | companies/contacts | collectFastContacts, runEnrich | VERIFIED unit |

## E. Shared processing

One evidence/contact model. Combinatorial IDs are `nation:industry:opportunity:crawler:mode`. Fan-out of the full catalog is forbidden (`fanOutAll: false`).

## F. Tests

| Test | Type | Result |
|------|------|--------|
| hive-coordinator.test.ts | unit | pass |
| hive-sync.test.ts | unit | pass |
| contact-supercrawl.test.ts | unit | pass |
| norr/fabric/orchestrator | unit | pass 111 |

## G. Metrics before / after

| Metric | Before | After |
|--------|--------|-------|
| Addressable hive engines | undeclared | 642411 |
| Engines run per company (normal, contacts already found) | fleet 8 + directories | 0 extra fleet |
| Engines run per company (email missing) | ~8–12 serial-adjacent | 4–6 parallel, 1600ms budget |
| Full catalog fan-out | possible via always-on supercrawl | never |

## H. Not yet verified

Live production 100-company wall-clock after this deploy.

## I. Next highest-value change

Measure production CORE_READY after deploy. Do not add more crawlers until that number is real.

## Honesty box

- Planned: hive mesh + skip fleet when CORE_SALES is done
- Implemented: yes
- Verified: unit
- Not production-ready because: wall-clock on norfai.com still needs this build live
