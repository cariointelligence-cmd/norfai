# Intelligence iteration report

- Project: Norf
- Date: 2026-09-13
- Environment: preview
- Cycle: Named Apify Actors as cost-capped enrichment, not search
- Skill version: 3.1

## A. User can now

Have three named Apify Actors fill published website / email / phone only after YTJ, free Finder HTML, harvest and other directories already ran. Official register identity is unchanged.

## B. Implemented

- `solidcode/finder-fi-scraper` — one-company Finder.fi lookup, max 3 rows, people off
- `agenscrape/finnish-business-finder-scraper` — same directory, technical fallback only
- `compass/crawler-google-places` — last-resort missing website, 1 place, reviews/leads/social off
- Input clamp so a crawl-sized request cannot start
- Pipeline persists matched contacts through identity + official-wins, then harvests a new origin
- Google Maps / Finder listing URLs are not stored as the company website

## C. Tests

| Test | Type | Result |
|------|------|--------|
| three named Actors, Face-closed catalog | unit | pass |
| skip when contacts already filled | unit | pass |
| plan Finder first, Maps only if website missing, never both Finders | unit | pass |
| clamp maxResults 100000 → 3; Maps scrapeContacts false | unit | pass |
| drop finder.fi and maps URLs; skip person records | unit | pass |
| Finder match skips Maps; primary fail → fallback Finder | unit | pass |
| official fact kept on conflict | unit | pass |

## Honesty box

- Planned: integrate the three named Actors into the existing enrich path, used only when needed
- Implemented: tool registry, cost gates, identity match, pipeline persist, junk-URL drop
- Verified: unit tests in this environment; live Actor runs not claimed from this process
- Not production-ready because: production must pick up this build; paid runs still depend on `APIFY_API` in the app environment
