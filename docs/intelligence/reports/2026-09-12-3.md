# Intelligence iteration report

- Project: Norf
- Date: 2026-09-12
- Environment: preview
- Cycle: Face empty-result diagnosis
- Skill version: 3.1

## A. User can now

See why a search stored zero companies: no match, register silence, filters excluded every candidate, out of coverage, or partial fill. The run page no longer shows only "No companies stored yet" when the register already wrote a reason code.

## B. Implemented

- Files: src/lib/norr/face-diagnosis.ts, sources/register-plan.ts, security.ts, pipeline.ts, actions.ts, novelty.ts, directories.ts, routes/_app/search/$runId.tsx
- Migrations: none
- Jobs: unchanged. Discover still writes source_report; finish() now stamps a register_plan code when homemade did not.

## C. Sources and repos

No new sources. This slice is Face + sanitizer + diagnosis correctness on the existing register path.

## D. Tests

| Test | Type | Result |
|------|------|--------|
| diagnose honest empty ≠ SOURCE_UNAVAILABLE | unit | pass |
| skip-seen → FILTER_EXCLUDED_ALL | unit | pass |
| sanitizer keeps code, hides adapter ids | unit | pass |
| Face maps sanitised report to copy | unit | pass |
| norr+security+search-hardening+ux+quota+engines+targeting | unit | 170 pass / 0 fail |

## E. Delta vs baseline

PRESENTATION_GAP: Cortex wrote `CODE: text` notes; sanitizer dropped them and Face showed a generic empty line. That hop is now wired.

Honest YTJ zero is NO_MATCH, not SOURCE_UNAVAILABLE.

## F. Incomplete or blocked

- Live Proff HTML drift (parser version 1)
- KEHA Työmarkkinatori
- Asiakastieto commercial REST
- Eval metrics (precision/recall) still null
- Finnish copy on the search-run page is still English, matching the rest of that view

## G. Next highest-value change

Keep YTJ pagination filling the requested count until the register is exhausted. Do not add licensed credit data.

## Honesty box

- Planned: Face diagnosis from register_plan notes
- Implemented: code on source_report, sanitizer preserve, run API summary.diagnosis, search-run UI
- Verified: unit tests in norr.test.ts and security.test.ts (this slice)
- Not production-ready because: eval-set live metrics are still empty; directory parsers remain trial
