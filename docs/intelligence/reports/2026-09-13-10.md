# Intelligence iteration report

- Project: Norf
- Date: 2026-09-13
- Environment: preview
- Cycle: Search Face — Email, Phone, Decision-maker visible while running
- Skill version: 3.1

## A. User can now

See Email, Phone and Decision-maker as first columns on a running search. Published values appear as they are stored. Empty fields say Pending while that company is still being enriched, then Not found. Tracker hosts such as try.abtasty.com are not shown as the company website.

## B. V1 gaps fixed

- Partial results Face had only Company / Y-tunnus / Website. Email/phone/decision-maker existed in the payload (106 emails already stored when 194 were missing) but were not columns. An older min-width + overflow-x table also hid those columns off-screen.
- getRun coalesced email from contacts but phone fell back to snippet-sourced values. Decision-maker SQL on the companies list still allowed `CLG Comm`.
- GET hydrate issued per-row phone deletes (N+1) on every poll.
- try.abtasty.com stayed on the Face as a website.

## C. Sources that actually work

| Id | Lifecycle | Independence group | Evidence | Status |
|----|-----------|--------------------|----------|--------|
| ytj | implemented | official_register | existing | VERIFIED |
| website harvest | implemented | company_site | existing | VERIFIED |
| contacts rollup | implemented | derived | getRun coalesce + refresh | VERIFIED |

## D. Engines wired

| Engine | Entrypoint | Persistent state | Callers | Status |
|--------|-----------|------------------|---------|--------|
| Face | `$runId.tsx` 7-column table + 6 stats | none | getRun | IMPLEMENTED |
| Identity | `publicWebsite` / `publicEmail` / `plausiblePersonName` | companies.website, contacts | getRun | IMPLEMENTED |
| Enrich | `refreshCompanyContactFields` skips duckduckgo/search_api phones | contacts, companies.phone | pipeline | IMPLEMENTED |

## E. Shared processing

Official facts still win. Search snippets remain URL evidence, not contacts. GET hydrate only nulls junk website/email already on the row (capped at 20 writes). No mass collection as a GET side effect.

## F. Tests

| Test | Type | Result |
|------|------|--------|
| contactFaceValue Pending vs Not found | unit | pass |
| try.abtasty.com is junk | unit | pass |
| norr + ux-hardening | unit | 83 pass / 0 fail |

Error paths covered: empty while running → Pending; empty after enriched → Not found; junk host stripped.

## G. Metrics before / after

| Metric | Before | After | n | Preliminary |
|--------|--------|-------|---|-------------|
| Partial results columns | 3 (Company, Y-tunnus, Website) | 7 (Company, Email, Phone, Decision-maker, Website, Y-tunnus, Status) | Face | yes |
| Contact stats | Missing email only | Email / Phone / Decision-maker found + missing | Face | yes |

## H. Not yet verified

Live production norfai.com still served the 3-column Face at screenshot time. This environment does not trigger that deploy.

## I. Next highest-value change

Keep enrich landing published email/phone/people onto the same rows the Face already reads. Do not invent mailboxes.

## Honesty box

- Planned: show Email, Phone, Decision-maker on running Partial results
- Implemented: 7-column Face, getRun phone/people coalesce, junk-site strip, Pending vs Not found, bounded GET cleanup
- Verified: 83 unit tests in this environment (contact Face + norr)
- Not production-ready because: production must pick up this build; empty contacts stay Pending/Not found until a published value exists
