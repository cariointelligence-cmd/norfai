# Intelligence iteration report

- Project: Norf
- Date: 2026-09-13
- Environment: preview
- Cycle: Apify reusable adapter layer (no Actors)
- Skill version: 3.1

## A. User can now

Have Apify available as a server-side collection layer inside Norf. No Actor runs until one is named. Official register search is unchanged.

## B. Implemented

- Server-only `APIFY_API` adapter: actor/task run, poll, dataset pagination, cache, retry, timeout, circuit, hourly budget, kill-switch
- Connector registry `APIFY_TOOLS` is empty by design
- Enrichment hook in the existing pipeline is a no-op until a tool is enabled
- Official facts win Apify conflicts; Face never receives the token or raw payloads
- Catalog entry is licensed/closed so Face search sources stay official-first

## C. Tests

| Test | Type | Result |
|------|------|--------|
| zero approved Actors; no network on enrich skip | unit | pass |
| missing secret does not start a run | unit | pass |
| kill-switch and circuit skip transport | unit | pass |
| sanitizer withholds APIFY_API / apify_api_ tokens | unit | pass |
| official name kept on conflict | unit | pass |

## Honesty box

- Planned: reusable Apify adapter that extends Norf and does not replace it
- Implemented: platform layer, registry, health, enrich hook, sanitizer
- Verified: unit tests in this environment; live `/users/me` not claimed here because this process does not always see the project secret
- Not production-ready because: zero Actors are approved; production must pick up this build before admin health can probe the platform
