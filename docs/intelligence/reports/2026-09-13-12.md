# Intelligence iteration report

- Project: Norf
- Date: 2026-09-13
- Environment: preview
- Cycle: Crawl fan-out — child jobs banned, in-process contact pages
- Skill version: 3.1

## A. User can now

A 300-company search queues at most one crawl job per company. Extra yhteystiedot/contact pages run inside that job (2 normal, 3 deep), in parallel. Leftover child crawl jobs from an already-running search are cancelled on the next worker tick.

## B. V1 gaps fixed

- Seed crawl still enqueued FAST_CONTACT_PATHS as child jobs after harvest already fetched those pages
- enqueueCrawl ignored cap, junk hosts, and duplicate seeds
- Harvest still seeded 12 contact paths then sliced; now the same short path list as the crawl job
- Child jobs on a live 6000-job run were only capped, not retired

## C. Sources that actually work

Unchanged: YTJ is the register. Harvest and the seed crawl read the company site. Apify remains cost-gated gap-fill.

## D. Engines wired

| Engine | Entrypoint | Change | Status |
|--------|-----------|--------|--------|
| L Orchestration | runCrawl / enqueueCrawl / pruneSearchWork | No child crawl jobs; prune retires payload.seed ≠ true | IMPLEMENTED |
| I Enrichment | followContactPagesInProcess + harvestSite | 2–3 in-process contact URLs; harvest budget 6/10 | IMPLEMENTED |
| C Acquisition | pickFanOutUrls | Canonical URL + short path list | IMPLEMENTED |

## E. Shared processing

Official facts still win. Crawl still writes website-sourced contacts only. Shared directory phones stay rejected.

## F. Tests

job-budget.test.ts: child-spawn lock, in-process follow count, canonical URL, pickFanOutUrls. 333 tests pass / 0 fail in this environment.

## G. Metrics before / after

| Metric | Before | After |
|--------|--------|-------|
| Crawl jobs per company | ~12–20 (seed + FAST_CONTACT_PATHS children) | 0 if contacts already found, else 1 |
| Contact pages after seed | queued as jobs | 2 (normal) / 3 (deep), fetched in parallel in the seed job |
| 300-company crawl queue | thousands | ≤300 |
| Harvest pages | 10 normal / 16 deep (then 12 paths) | 6 / 10, same path policy |

## H. Not yet verified

Drain of production run a4c42e1b-… happens when this build is serving that workspace. pruneSearchWork retires queued children (`seed` ≠ true) on the next tick. Not claimed against norfai.com from this sandbox.

## I. Next highest-value change

Do not add Actors. If a firm still has no site after enrich, keep scrape + one seed crawl.

## Honesty box

- Planned: crawl jobs must not spawn crawl jobs; extra contact pages stay in-process
- Implemented: policy lock, in-process follow, cap-aware enqueue, harvest path list, child-job prune
- Verified: unit tests in this environment
- Not production-ready because: production must pick up this build; leftover children on a live run are drained by prune after deploy
