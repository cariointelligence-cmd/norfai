# Intelligence iteration report

- Project: Norf
- Date: 2026-09-13
- Environment: preview
- Cycle: Unstick 3-hour search — crawl fan-out, score gate, shared phone
- Skill version: 3.1

## A. User can now

A 300-company search finishes after enrich + one short contact crawl, not after thousands of page jobs. Leftover crawls on an already-running search are cancelled once contacts exist or the time budget is up. Scoring is not blocked by extra website pages. The shared `+35810665101` number is not shown as a company phone.

## B. V1 gaps fixed

- Job explosion: enrich + scrape each seeded a 24-page crawl (CONTACT_SEED_PATHS + sitemap + link fan-out) → ~20 jobs/company
- scoreMayProceed waited for every crawl/signals job, so the run sat on "Reading websites"
- Run completed only when queued+running = 0
- getRun shipped every job row to the Face (6396 rows / 2s)
- runScrape stored search-snippet emails and phones (search_api)
- TED/Hilma signals ran on every standard search
- MX DNS on every score even when a published mailbox existed
- Shared directory phone leaked onto Automaalaus and others

## C. Sources that actually work

Unchanged: YTJ is the register. Harvest still reads the company site. Apify remains cost-gated gap-fill.

## D. Engines wired

| Engine | Entrypoint | Change | Status |
|--------|-----------|--------|--------|
| L Orchestration | pruneSearchWork + job-budget | Cap 4 crawls (8 deep); cancel extras | IMPLEMENTED |
| I Enrichment | runEnrich / runCrawl / runScrape | Harvest first; scrape only if no site; no snippet contacts | IMPLEMENTED |
| K Quality | scoreMayProceed | Score after enrich/scrape, not after every page | IMPLEMENTED |
| Face | getRun jobCounts | Progress from aggregates; 40-row job preview | IMPLEMENTED |

## E. Shared processing

Official facts still win. Snippet phones/emails are not stored. Shared numbers used on 2+ other firms are dropped.

## F. Tests

See job-budget.test.ts + scrape-gate + runtime + progress. 173 tests pass.

## G. Metrics before / after

| Metric | Before | After |
|--------|--------|-------|
| Jobs for 300 companies | ~6000+ | ~900 core (enrich+score) + ≤4 crawls only if contacts missing |
| Score blocked by crawl | yes | no |
| Run complete condition | all jobs including extra crawls | extras cancelled when contacts found or 20 min after enrich |
| Face job payload | all rows | counts + 40 |

## H. Not yet verified

Live drain of production run a4c42e1b-… happens when this build is serving that workspace. Not claimed from this sandbox against norfai.com.

## I. Next highest-value change

If a company still has no site after enrich, keep scrape+one crawl. Do not add more Actors.

## Honesty box

- Planned: stop the 3-hour stuck search without inventing contacts
- Implemented: crawl cap, prune, score gate, snippet drop, shared-phone drop, Face job summary
- Verified: unit tests in this environment
- Not production-ready because: production must pick up this build; the live 6396-job run is drained by prune on the next worker/tick after deploy
